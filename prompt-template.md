# 黑盒测试提示词模板（填空即用）

> 硬性纪律（红线、等待基线、批次纪律、结束闸门）已内置在 `web-blackbox-testing/SKILL.md`
> 的「任务启动契约」与「必守清单」里，**提示词不需要重复**。

## 标准功能测

```
【触发技能】web-blackbox-testing
【环境】MES_URL=____；MES_ACCOUNT=____；MES_PASSWORD=____
【需求文档】<本地路径或链接>
【测试深度】标准功能测
【覆盖范围】____（多功能时限定，如「仅 XX 功能」）
【排除范围】____
【ONES 工单】____（需要提缺陷/去重/回归流转时给）
【交付物】用例 / 缺陷清单 / 测试报告；是否提 ONES：____
```

## 快速核心流

```
黑盒测试 <URL>（<账号>/<密码>），需求 <文档>，快速核心流，覆盖 <范围>，排除 <范围>。
```

## 仅提缺陷 / 仅造数 / 修复回归

```
提缺陷：工单 <URL>，清单 bug-reports/<文件>.md，profile <项目名>，前端类加 --handler frontend。
造数：按 references/master-data-setup.md 造 产品 + 工艺路线 + BOM，命名带测试标识（bbt_osd_setup.py）。
修复回归：缺陷单 <编号/链接>，本轮只验证修复项 + 相邻影响面，结果写回清单「回归验证」段。
```

## 备注

- 环境也可直接给 URL/账号/密码，登录会按页面 URL 自动定位，不要写死进 skill。
- 不写「测试深度」默认按标准功能测；只想先判断能不能用才写「快速核心流」。
- **启动闸门**由技能内置：长任务首次会先输出 ≤10 行计划等确认；短任务/回归/补测直接跑。
