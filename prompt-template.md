# 黑盒测试提示词模板（复用，省 token）

> 用法：复制对应模板，填空后直接发。技能（web-blackbox-testing 等）会自动触发并照 skill 固化流程执行。
> 版本要点（2026-09-03 修订）：所有硬性纪律（不下载浏览器/不重写脚本/不匹配即停/侦察≤2次/只做标准操作/失败分级/结束闸门）已收进
> `web-blackbox-testing/SKILL.md` 顶部「必守清单（执行前先对照）」，**提示词不再重复这些长规则**，只保留任务参数与「启动闸门」。

## 一、完整版（开新任务推荐）

```
【触发技能】web-blackbox-testing（造数/提缺陷自动衔接 generate-manufacturing-test-cases / ones-create-linked-defect）
【任务类型】开始黑盒测试 / 补测 / 修复回归 / 仅提缺陷
【环境】MES_URL=____；MES_ACCOUNT=____；MES_PASSWORD=____（浏览器统一用本机系统 Chrome，按 skill 必守清单执行）
【需求文档】本地文件路径（最常见，如 /Users/xxx/需求.docx）；也支持可访问链接：____
【测试深度】标准功能测 / 深度专项测 / 快速核心流（不写默认标准功能测）
【覆盖范围】可写「按需求文档全流程测」；文档含多个功能时建议限定（如【仅 XX 功能】）。具体：____
【排除范围】如「导入本轮不测，后续补」：____
【造数】需要造前置数据吗？需要就给页面与字段，或直接写：
   按 references/master-data-setup.md 造：产品 + 工艺路线(工序) + BOM，命名带测试标识（bbt_osd_setup.py）。
【ONES 工单】URL（需要提缺陷/去重/回归流转时给）：____
【交付物】测试用例 / 缺陷清单 / 测试报告 / 是否提 ONES：____
【启动闸门】动手长任务前，先输出 ≤10 行执行计划：
   0) 环境勘察（本机浏览器/playwright/站点连通性）→ 1) 读需求+skill → 2) 生成标准用例
   → 3) 单次登录+单会话单长脚本执行 → 4) 报告/缺陷清单/数据清理/ONES 提缺陷。计划未经我确认不得进入第 3 步。
【执行要求】执行前先对照 web-blackbox-testing/SKILL.md 顶部「必守清单（执行前先对照）」，逐条自检后再动手；
   与 ONES 工单已关联 bug 去重，只提新增；待确认/脏数据项按我说明忽略或先问我。
【最终回复】只给结论、缺陷编号+严重程度、文件绝对路径、数据清理结果、下一步建议，不贴长正文。
```

> **填写备注**
> - 【环境】站点/账号按任务给（`MES_URL` 等）；若不想设环境变量，直接写 URL/账号/密码即可，登录会按页面 URL 自动定位，不要写死进 skill 脚本。
> - 【测试深度】不写默认「标准功能测」；只想先判断能不能用才写「快速核心流」。
> - 【覆盖范围】不写默认按快速核心流处理；想全流程测就写「按需求文档全流程测」（文档含多功能时建议【仅 XX 功能】）。
> - 【启动闸门】是防“闷头跑偏”的关键：第一次使用建议只发到计划为止，等 AI 复述 0~4 步与将复用的脚本后再放行执行。
> - 其余硬性纪律（不重复侦察、不重写脚本、只做标准操作、不匹配即停、结束先清理等）都在 skill 顶部必守清单里，不必在提示词重复。

## 二、精简版（一个自然段即可）

```
黑盒测试 <URL>（<账号>/<密码>），需求 <文档>，标准功能测，覆盖 <范围>，排除 <范围>。
执行前先对照 web-blackbox-testing/SKILL.md 顶部必守清单。造数按 references/master-data-setup.md（带测试标识）。
提缺陷工单 <URL>，与已关联去重，只提新增；缺工单 URL 先问我。
最终只给结论、缺陷、文件路径、数据清理结果。
```

## 三、仅提缺陷

```
提缺陷：工单 <URL>，清单 bug-reports/<文件>.md，profile <项目名>，
与工单已关联去重，只提新增；待确认/脏数据忽略。
```

## 四、仅造数

```
造前置数据：按 references/master-data-setup.md，
产品信息定义造产品（名称+编号）、工艺路线定义造工艺路线+工序、产品BOM造 bom产品/母件产出量/子件消耗量，
命名带测试标识，一次登录造完（bbt_osd_setup.py；站点/账号用 MES_URL/MES_ACCOUNT/MES_PASSWORD 环境变量）。
```

## 为什么省 token
- 硬性纪律已固化到 skill 顶部「必守清单」，提示词只给任务参数（站点/账号/需求/深度/范围/造数/工单），不再重复长规则。
- 保留「启动闸门」（先出计划再放行）这一条提示词专属机制，能低成本提前纠偏。
- 明确“与工单去重、忽略待确认、缺 URL 先问、结束先清理”，避免无效提缺陷与脏数据残留。
- 明确“只给结论/缺陷/路径”，避免长篇输出。
