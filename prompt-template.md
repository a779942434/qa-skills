# 黑盒测试提示词模板（复用，省 token）

> 用法：复制对应模板，填空后直接发。技能（web-blackbox-testing 等）会自动触发并照 skill 固化流程执行，不需要解释背景。

## 一、完整版（开新任务推荐）

```
【触发技能】web-blackbox-testing（造数/提缺陷自动衔接 generate-manufacturing-test-cases / ones-create-linked-defect）
【任务类型】开始黑盒测试 / 补测 / 修复回归 / 仅提缺陷
【站点】URL：________
【账号】________ / ________
【需求文档】本地文件路径（最常见，如 /Users/xxx/需求.docx）；也支持可访问链接：________
【测试深度】快速核心流 / 标准功能测 / 深度专项测（不写默认标准功能测）
【覆盖范围】可写「按需求文档全流程测」；文档含多个功能时建议限定（如【仅 XX 功能】）。具体：________
【排除范围】如「导入本轮不测，后续补」：________
【造数】需要造前置数据吗？需要就给页面与字段，或直接写：
   按 references/master-data-setup.md 造：产品 + 工艺路线(工序) + BOM，命名带测试标识
【ONES 工单】URL（需要提缺陷/去重/回归流转时给）：________
【交付物】测试用例 / 缺陷清单 / 测试报告 / 是否提 ONES：________
【执行要求】
- 直接按 skill 固化流程执行：登录一次（bbt_osd_common.login_ousida）、新页面用 recon-generic/recon_page.py 侦察并固化、造数用 bbt_osd_setup.py、提缺陷用 ones_submit_defects.py。
- 不要重复侦察同一页面、不要重写登录/导航/下拉脚本，不输出无关大段 dump。
- 与 ONES 工单已关联 bug 去重，只提新增；待确认/脏数据项按我说明忽略或先问我。
- 最终回复只给结论、缺陷、文件路径、下一步，不贴长正文。
```

> **填写备注**
> - 【测试深度】不写默认「快速核心流」：只测主链路 + 关键校验 + 明显缺陷，适合先判断能不能用。
> - 【覆盖范围】不写默认按快速核心流处理；想全流程测就写「按需求文档全流程测」（文档含多功能时建议【仅 XX 功能】）。
> - 想要又快又全：覆盖范围写「按需求文档全流程测」，深度写「标准功能测」。

## 二、精简版（一个自然段即可）

```
黑盒测试 <URL>（<账号>/<密码>），需求 <文档>，标准功能测，覆盖 <范围>，排除 <范围>。
造数按 references/master-data-setup.md（产品+工艺路线+工序+BOM，带测试标识）。
提缺陷工单 <URL>，与已关联去重，只提新增；待确认/脏数据忽略。
最终只给结论、缺陷、文件路径。
```

## 三、仅提缺陷

```
提缺陷：工单 <URL>，清单 bug-reports/<文件>.md，profile <项目名>，
与工单已关联去重，只提新增；待确认/脏数据忽略。
```

## 四、仅造数

```
造前置数据：按 references/master-data-setup.md，
产品信息定义造产品（名称+编号）、工艺路线定义造工艺路线+工序、产品BOM造 bom产品/母件产出量/子件消耗量，
命名带测试标识，一次登录造完（bbt_osd_setup.py）。
```

## 为什么省 token
- 参数一次给全（站点/账号/需求/深度/范围/造数/工单），不用来回追问。
- 明确“按固化流程执行、不重复侦察/重写脚本”，避免 AI 反复 dump 和试错。
- 明确“与工单去重、忽略待确认”，避免无效提缺陷。
- 明确“只给结论/缺陷/路径”，避免长篇输出。
